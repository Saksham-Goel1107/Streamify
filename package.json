{
  "name": "streamify2",
  "private": true,
  "scripts": {
    "install-deps": "cd frontend && npm install --include=dev && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
